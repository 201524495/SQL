/**
SELECT * FROM USER_CONSTRAINTS
WHERE TABLE_NAME='DEPT';
*/

SELECT *
FROM ALL_CONS_COLUMNS
WHERE CONSTRAINT_NAME = (SELECT CONSTRAINT_NAME 
        FROM ALL_CONSTRAINTS
        WHERE TABLE_NAME = 'DEPT'
        AND CONSTRAINT_TYPE = 'P');

SELECT CONSTRAINT_NAME, TABLE_NAME, STATUS
FROM user_constraints
WHERE TABLE_NAME='DEPT';

ALTER TABLE DEPT
DISABLE CONSTRAINT DEPT_PRIMARY_KEY CASECADE;

/*
ALTER TABLE DEPT
DISABLE CONSTRAINT DEPT_PRIMARY_KEY CASCADE;

SELECT CONSTRAINT_NAME, TABLE_NAME, STATUS
FROM USER_CONSTRAINTS
WHERE CONSTRAINT_NAME LIKE 'DEPT%' AND OWNER = 'TEST';

ALTER TABLE DEPT
ENABLE CONSTRAINT DEPT_PRIMARY_KEY CASCADE;

SELECT CONSTRAINT_NAME, TABLE_NAME, STATUS
FROM USER_CONSTRAINTS
WHERE CONSTRAINT_NAME LIKE 'DEPT%';*/