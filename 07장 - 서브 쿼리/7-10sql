/**
TAKE'S JOB : TESETR
BLACK'S SAL : 2850
*/

SELECT EM.ENAME, EM.JOB, EM.DEPTNO, EM.SAL
FROM EMP EM
WHERE (EM.JOB = (SELECT E.JOB FROM EMP E WHERE E.ENAME = 'Take' )) 
    OR (EM.SAL >= (SELECT E.SAL FROM EMP E WHERE E.ENAME = 'Black'))
ORDER BY JOB, SAL DESC;